// import Swiper from 'swiper/bundle'; 
//  import Swiper from "swiper-bundle.min.js";

const swiperGraffiti = new Swiper('.work__swiper', {
    // Optional parameters

    loop: true,
    spaceBetween:0.1,
    grabCursor:true,
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
      dynamicBullets:true,
      clickable:true,

    },

  })

  const swiperZombie = new Swiper('.work__swiper__zombie', {
    // Optional parameters

    loop: true,
    spaceBetween:0.1,
    grabCursor:true,
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
      dynamicBullets:true,
      clickable:true,

    },

  })

  const swiperGruffalo = new Swiper('.work__swiper__gruffalo', {
    // Optional parameters

    loop: true,
    spaceBetween:0.1,
    grabCursor:true,
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
      dynamicBullets:true,
      clickable:true,

    },

  })
  
  const swiperAudio = new Swiper('.work__swiper__audio', {
    // Optional parameters

    loop: true,
    spaceBetween:0.1,
    grabCursor:true,
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
      dynamicBullets:true,
      clickable:true,

    },

  })

  const swiperChaThai= new Swiper('.work__swiper__ct', {
    // Optional parameters

    loop: true,
    spaceBetween:0.1,
    grabCursor:true,
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
      dynamicBullets:true,
      clickable:true,

    },

  })

  const swiperLetter= new Swiper('.work__swiper__letter', {
    // Optional parameters

    loop: true,
    spaceBetween:0.1,
    grabCursor:true,
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
      dynamicBullets:true,
      clickable:true,

    },

  })

  const swiperLost1= new Swiper('.work__swiper__lost1', {
    // Optional parameters

    loop: true,
    spaceBetween:0.1,
    grabCursor:true,
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
      dynamicBullets:true,
      clickable:true,

    },

  })

  const swiperLost2= new Swiper('.work__swiper__lost2', {
    // Optional parameters

    loop: true,
    spaceBetween:0.1,
    grabCursor:true,
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
      dynamicBullets:true,
      clickable:true,

    },

  })